# Phase 2 External Constraint 固定回归测试清单


> 历史执行记录：以下原使用规则、预期、状态和日期事实原样保留，不是当前有效规则或当前工作树结论。2026-09-11已追加[证据复核](evidence-review-2026-09-11.md)，其中分层复核与NOT_RUN纠偏不覆盖旧事实；当前验收预期见[AC清单](../requirements/acceptance-checklist.md)，执行纪律只见[AGENTS.md](../../AGENTS.md)。历史代码版本/需求版本未完整记录，不推定对应本次v1。

## 使用规则

- 基线来源：生命周期需求的 External Constraint（Phase 2）章节及验收清单的 External Constraint 补充。
- Constraint 是轻量外部治理对象，不能作为 Stage 或并行事项的替代；推荐 Stage 只可推荐/筛选，不可阻止建立。
- 每条写用例均验证数据库/API 投影、即时 UI 回显与刷新后存在。隔离数据前缀为 `REG-P2-YYYYMMDD-`。

| ID | 模块 | 前置条件 | 操作步骤 | 预期结果 | 自动化 | 当前执行结果 |
| --- | --- | --- | --- | --- | --- | --- |
| P2-001 | 零约束默认 | 新隔离项目，无适用阻断约束 | 查看列表、详情和 API 后刷新 | `external_constraints_cleared=true`，文案为外部条件已具备 | API+UI | PASS |
| P2-002 | 阻断约束建立 | 新项目与阻断模板/临时约束 | 建立约束，确认范围 | 未解除阻断约束使 cleared=false；当前 UI 与刷新一致 | API+UI | PASS |
| P2-003 | 办理状态流转 | 已建立约束 | 依次开始办理、记录进展、登记结论、失效 | UI/API 显示未开始、办理中、已形成结论、结论失效；过程细节写入 Progress Log，每次有审计 | API+UI | PASS（2026-09-10 自动化 + 浏览器快速办理入口） |
| P2-016 | 约束进展记录 | 已建立约束 | 新增、编辑、软删除约束进展 | 默认隐藏软删除记录；完整审计保留原文、操作人和原因 | API+UI | 自动化通过；待浏览器回归 |
| P2-017 | 批量办理 | 多项目具备同一约束模板 | 预检、批量开始/进展/解除 | 不合格对象有原因且无部分成功；解除同时有总审计和逐项目审计 | API+UI | 自动化通过；待浏览器回归 |
| P2-018 | 阶段跟踪约束列 | 有 External Constraint 的项目 | 添加、排序、点击约束列 | 列与 Work Item 明确区分；点击进入快速办理；偏好刷新后保持 | UI | PASS（2026-09-10 浏览器验证） |
| P2-019 | 阶段跟踪列级批量办理 | 多项目已有同一 Work Item / Constraint 实例 | 点击业务列表头、逐项取消、预检并批量办理 | 仅选择可办理真实实例；切换列清除原选择；总审计和逐项目审计完整 | API+UI | API PASS；浏览器 BLOCKED（列级批量写入与刷新持久化待执行） |
| P2-004 | 解除三态 | 阻断约束 | 分别登记未解除、已解除、不适用 | 仅已解除/不适用退出阻断；不适用须理由且非重新阻断 | API+UI | PASS |
| P2-005 | 非阻断约束 | 新项目 | 建立并确认非阻断约束 | cleared 仍为 true；详情仍显示该约束 | API+UI | PASS |
| P2-006 | 预算核定 | 新项目，初始预算 | 建立预算核定，登记已解除结论并指定当前来源 | effective_budget=核定预算，source=budget_constraint；列表/详情/导出一致 | API+UI | PASS |
| P2-007 | 预算来源原子切换 | 两条已解除预算结论 | 从甲切换至乙，刷新并查审计 | 同时仅一条当前来源；预算切换无中间错误投影 | API+UI | PASS |
| P2-008 | 有效预算回退 | 当前预算来源约束 | 令当前结论失效或取消当前来源 | 按历史审核预算、再初始预算回退；source 可解释 | API+UI | PASS |
| P2-009 | 模板范围预检 | 不同 project_type 与日期项目 | 按范围建立模板后预检/批量应用 | 仅命中适用对象；推荐 Stage 不阻断手工建立 | API+UI | PASS |
| P2-010 | 模板归档恢复 | 已引用的外部约束模板 | 归档、查看选择器、恢复 | 归档不在默认选择器；既有实例/快照不变；恢复后可选 | API+UI | PASS |
| P2-011 | 批量临时约束 | 勾选项目 | 新增临时约束，分别不保存/保存为常用模板 | 仅明确保存时沉淀模板；项目实例均持久化 | API+UI | PASS |
| P2-012 | 工作台筛选 | 有未解除阻断和已解除/非阻断项目 | 点击“外部条件待处理” | 只显示存在未解除阻断约束项目；筛选前选择保留 | API+UI | PASS |
| P2-013 | Hero Stats/摘要 | 具有上述项目 | 比对顶部主统计、次级提示、列表 | 主统计与次级“待处理”分层；数量/预算使用服务端投影 | API+UI | PASS |
| P2-014 | 管理时间线 | 已建立、结论、预算的项目 | 打开详情时间线及完整审计 | 仅治理事实和高亮进展进时间线；细碎编辑只在完整审计 | API+UI | PASS |
| P2-015 | 刷新闭环 | 任一 Phase 2 写操作 | API 验证后浏览器强制刷新 | 约束状态、解除结果、预算、摘要和时间线均仍一致 | UI | PASS |

## 2026-09-07 首轮执行证据

- 自动化：`python -m pytest backend/tests -q`，64 passed；其中 External Constraint 测试覆盖零约束、阻断/非阻断、预算来源切换与回退、范围、模板生命周期、筛选和时间线 API 投影。
- 浏览器：建立 `REG-P2-20260907 预算核定` 阻断约束，验证未解除、开始办理、登记解除结论与刷新持久化。
- 已记录失败：P2-014 的结论事件已出现在 API 时间线，但当前详情在写入后未即时回显，刷新后才出现。

### 2026-09-07 自动化执行结果补记

以下先前待执行项已由本轮 `backend/tests`（64 passed）覆盖并判定 PASS：P2-003、P2-005、P2-006、P2-007、P2-008、P2-009、P2-010、P2-011、P2-012、P2-013。

### 2026-09-09 回归修复验证

- P2-014：浏览器登记 `REG-P2-20260907 预算核定` 的 120 万解除结论后，外部约束卡片、当前有效预算来源与“形成外部约束结论”管理时间线事件均无需刷新立即回显；刷新详情后投影一致。

### 2026-09-10 工作台办理回归

- 自动化：` .\.venv\Scripts\python.exe -m pytest backend\tests -q`，**74 passed**。新增覆盖约束 Progress Log 新增、编辑、软删除及审计；非阻断约束不计入待处理；批量预检、批量解除总审计/逐项目审计，以及预算核定必须逐项目登记。
- 浏览器：在“测试项目1”的“浏览器验收外部约束”中，经工作台“外部条件待处理”摘要进入约束列表和快速办理，新增“浏览器回归：已核验材料”进展后即时显示；新开工作台页面后该进展仍存在。
- 浏览器：阶段跟踪“显示列”可将“浏览器验收外部约束”作为带“约束”来源标识的列加入；单元格显示办理中、待解除和最近进展，点击后直接进入约束快速办理。
- P2-016 的浏览器编辑/软删除、P2-017 的浏览器批量办理仍保留为待执行的专项回归，不能由本次自动化结果替代。

### 2026-09-10 阶段跟踪列级批量办理

- 自动化：` .\.venv\Scripts\python.exe -m pytest backend\tests -q`，**77 passed**。新增 Work Item 精确实例预检/原子执行、批量总审计与逐项目进展审计，以及 External Constraint 的项目加实例 ID 精确目标回归。
- 浏览器：当日早些时候 `5173/5174` 端口绑定返回 `EACCES`；已改用本机可用的 `http://127.0.0.1:5214/`。已验证 Work Item 与 External Constraint 单元格可直接进入快速办理，以及列头选择、逐项取消后数量与单元格高亮同步。尚未在浏览器执行两实例的实际批量写入及刷新持久化，因此 P2-019 仍为 BLOCKED；不得以本次无写入交互验证替代。

### 2026-09-15 外部约束解除与预算影响收敛（当前工作树，未提交）

- 需求版本：REQ-EC-001@v4、REQ-EC-002@v3、REQ-EC-003@v2、REQ-BULK-001@v5、REQ-BULK-002@v4、REQ-STAT-001@v3；本节只记录本次实际证据，不覆盖以上历史执行事实。
- API：` .\.venv\Scripts\python.exe -m pytest backend\tests\test_external_constraints.py -q`，**21 passed**。覆盖任意未解除约束（含历史非阻断实例）计入待处理、单一解除动作的可选结论/说明、影响有效预算时的必填校验与来源投影、其他影响说明校验、历史 manual 范围兼容以及批量预算影响约束的逐项目金额与回滚。
- 关联 API：` .\.venv\Scripts\python.exe -m pytest backend\tests\test_external_constraints.py backend\tests\test_pmo_operations.py backend\tests\test_projects_crud.py -q`，**48 passed**。包含项目库范围的“外部条件待处理”筛选，避免 Hero 统计与列表 Stage 范围不一致。
- Browser（只读入口检查，`http://localhost:5173/`）：工作台“管理常用外部约束”可见影响范围三项，未见阻断、日期或手工范围字段；项目详情的两个未结束约束均可直接展开“解除约束”，表单仅呈现可选结论/说明和确认入口。未用用户数据库执行解除写入，因此浏览器层的保存→刷新持久化仍为 **NOT_RUN**，不得由 API 或只读检查替代。
- Browser（筛选路径）：点击“外部条件待处理：2 个项目 · 涉及预算 110 万”后，导出链接与列表请求均为 `group=project_library&external_conditions=ongoing`，列表即时显示 2 个对应项目及其“外部约束待处理”摘要，不再保留未立项范围而错误显示 0 条。
- 构建/文档：`npm.cmd --prefix frontend run build` 通过；` .\.venv\Scripts\python.exe scripts\check_docs_governance.py --root .` 通过（仅结构检查，不能证明业务或浏览器验收）。

### 2026-09-22 预算影响约束解除字段回归（当前工作树，未提交）

- 需求版本：REQ-EC-002@v4、REQ-EC-003@v3、REQ-UI-005@v9。本次修复模板实例可被客户端 `impact_scope=none` 覆盖的问题；模板影响范围现以实例快照为权威。
- API：` .\.venv\Scripts\python.exe -m pytest backend\tests\test_external_constraints.py -q`，**23 passed**。新增模板预算影响继承和真实 `0` 有效预算解除用例；既有缺少有效预算拒绝用例继续通过。
- Build：`npm.cmd --prefix frontend run build` 通过。
- Browser：工作台与项目详情均已打开“预算审核影响预算”约束，确认上下文显示“影响范围：影响有效预算”，解除表单显示“解除后的有效预算（万元）*”；工作台空提交会聚焦该字段并显示错误。为避免改动现有项目数据，实际“输入金额→解除保存→刷新”写入链路仍为 **NOT_RUN**；API、构建和只读/校验浏览器证据不能替代该验收。

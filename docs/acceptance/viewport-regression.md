# 工作台视口固定回归测试清单


> 历史执行记录：以下原使用规则、预期、状态和日期事实原样保留，不是当前有效规则或当前工作树结论。2026-09-11已追加[证据复核](evidence-review-2026-09-11.md)，其中分层复核与NOT_RUN纠偏不覆盖旧事实；当前验收预期见[AC清单](../requirements/acceptance-checklist.md)，执行纪律只见[AGENTS.md](../../AGENTS.md)。历史代码版本/需求版本未完整记录，不推定对应本次v1。

## 通用前置条件

工作台有足以产生项目表溢出、Stage 卡片与两个已选项目的数据；一次在“批量操作”及一次在“快速办理”展开状态下检查。每项记录浏览器截图，并检查 `document.documentElement.scrollWidth <= window.innerWidth`（组件自身横向滚动容器除外）。

| ID | 尺寸 | 操作步骤 | 预期结果 | 自动化 | 当前执行结果 |
| --- | --- | --- | --- | --- | --- |
| VP-001 | 1440×900 | 总览、展开批量、展开快速办理 | 无页面级横滚；表/Stage 的独立滚动不溢出；控件不裁切 | Browser | PASS |
| VP-002 | 1366×768 | 同 VP-001，滚动页面到底 | 项目、进展、下一节点可读；右侧完整；无空白面板或文字重叠 | Browser | PASS |
| VP-003 | 1100×768 | 打开批量及快速办理，检查关闭、遮罩、Esc | 覆盖面板≥320px，不压缩项目表为窄条；可完整收起 | Browser | PASS |
| VP-004 | 900×768 | 展开操作台并检查文档顺序 | Stage→项目列表→操作台→导入区；导入在最下方 | Browser | PASS |
| VP-005 | 768×1024 | 总览、Stage 横带、详情快速办理 | 无页面级横滚/字符级断行/裁切；项目表可自身横滚 | Browser | PASS |
| VP-006 | 600×900 | 同 VP-005，滚动全页 | 单列顺序不变；Stage 只横向滚动且卡片无纵向内滚 | Browser | PASS |
| VP-007 | 全尺寸 | 在中屏快速办理后返回批量 | 原筛选、选择集合和批量操作模式保留 | Browser | BLOCKED（待执行） |

## 2026-09-07 首轮执行证据

- 1440、1366、1100、900、768、600 均测得页面 `scrollWidth <= innerWidth`；1100px 覆盖式操作台宽 360px，Esc 可收起。
- VP-006 FAIL：600×900 截图显示 sticky 操作台仍视觉占用项目表右侧，违反窄屏单列不变量。
- VP-007 仍待以“快速办理”而非详情返回路径复现；当前没有技术或数据限制。

VP-007 仍为 BLOCKED：尚未在中屏完成“从工作台快速办理进入、再返回批量操作台”的专门路径；此前验证的是详情返回，不能替代该用例。

### 2026-09-09 回归修复验证

- VP-006：真实浏览器 600×900 下，工作台按 Stage 带 → 项目表 → 批量操作 → 导入区顺序呈现；Stage 卡仅在自身横向带滚动，项目表仍可操作，操作台不再以 sticky 侧栏挤占项目表。

### 2026-09-09 项目列表主体高度验证

- 宽屏真实数据视图：8 条项目时 `board` 随真实表格内容增长；筛选到 1 条项目或 0 条项目时，`board` 均保持 `648px` 主体高度，导入区起点保持在 `1058px`，未随项目行减少继续上移。0 条结果渲染“当前条件下暂无项目”。
- 结构性视口回归：`1920×1080` 下 Header/Workspace 均为 `1720px`，主列与操作台间距为 `24px`，`board` 为 `722px`；`900×768`、`768×1024`、`600×900` 下 `board` 分别为 `384px`、`640px`、`516px`，顺序保持 Stage → 项目表 → 操作台 → 导入区，且 `scrollWidth <= innerWidth`。
- 待人工数据矩阵：尚未在每个目标视口逐一重复 8、2、1、0 条真实项目组合；本轮仅记录上述真实数据与结构性视口证据，不将该完整矩阵标记为通过。

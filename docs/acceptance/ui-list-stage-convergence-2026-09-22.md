# 工作台列表与阶段跟踪收敛定向验证（2026-09-22）

- **运行标识：** `UI-LIST-STAGE-20260922`
- **需求 / 验收：** `REQ-PRJ-002@v2`、`REQ-PRJ-003@v7`、`REQ-UI-003@v11`、`REQ-UI-004@v6`、`REQ-UI-006@v10`、`REQ-UI-007@v4`、`REQ-UI-008@v3`；`AC-006`、`AC-019`、`AC-020`、`AC-022`、`AC-023`、`AC-024`、`REG-UI-001`。
- **代码状态：** 工作树已有跨 Phase 的未提交改动；本记录不把历史 PASS 推定为当前结果。
- **环境：** Windows 本机，FastAPI `127.0.0.1:8000`、Vite `http://localhost:5173`，真实 Edge 浏览器。

| 层级 / 用例 | 结果 | 实际观察与证据 |
| --- | --- | --- |
| 默认排序与导出同源查询 | PASS | `backend/tests/test_projects_crud.py` 与 `backend/tests/test_pmo_operations.py`：29 passed。新增回归覆盖同部门、同实施年份按项目分类配置顺序再按编号排序，以及无实施年份回退。 |
| 前端类型与生产构建 | PASS | `npm.cmd --prefix frontend run build` 成功。 |
| 文档引用与回归基线 | PASS | `tests/test_docs_governance.py`：26 passed；`scripts/check_docs_governance.py --root .` 成功。`REG-UI-001` 已加入整体回归表。 |
| 总览列集与首列表头选择 | PASS | 浏览器打开未立项 Stage：预算后显示独立项目概括，立项文件、合同和正式分配未出现；“显示列”提供恢复默认入口。点击首列表头可全选当前 21 条，再次点击可清空。 |
| 阶段标题批量入口与单元格状态 | PASS | 浏览器中业务列表头为直接按钮，单击进入手动选择且初始为 0 项；双击 PMO 审核列仅选中 19 个真实可办理实例（已完成实例未混入）。单元格可直接办理，重点关注仅出现在状态文字前。 |
| 详情入口与独立概况字段 | PASS | 从工作台进入项目详情，项目概况分别显示项目分类、地点和采购属性，未再依赖组合摘要。 |
| 8/2/1/0 条数据、短/长操作台与全部目标视口 | NOT_RUN | 未在本次定向验证逐项构造数据并执行 `1440×900`、`1100×768`、`900×768`、`768×1024`、`600×900` 的完整浏览器矩阵；不得据此标记 `AC-023`、`AC-024` 或 `REG-UI-001` 整体 PASS。 |
| 详情滚动下固定页头不遮挡内容 | NOT_RUN | 已验证详情正常入口和独立字段；尚未在长详情进行滚动截图/边界测量。 |

## 本次新增 / 更新的回归基线

- 新增后端排序回归：`test_default_list_sort_uses_project_type_between_year_and_project_code`。
- 新增整体浏览器回归条目：`REG-UI-001`，覆盖稳定主体、各 Stage 列集、总览偏好、表头选择、阶段标题单击/双击、基础配置拖拽、排序、详情固定页头与合同列密度。

## 阶段性回归待办

完成完整回归窗口时，按 `REG-UI-001` 逐个构造 8、2、1、0 条结果及短/长操作台，并以真实截图和容器边界记录五个常规视口；同时验证详情滚动固定页头和总览偏好刷新持久化。
